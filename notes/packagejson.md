It keep tracks of ur all project which dependices which verision of particular package or react itself is used by this it is handled by package.json 

Why we need not push node modules on github 
all information that are used in ur project that is include in package.json 